# Allow/Deny Authorization Configuration
# Demonstrates explicit allow and deny rules

port: 4223

authorization: {
  users = [
    {
      user: admin
      password: admin123
      permissions: {
        publish: ">"
        subscribe: ">"
      }
    }
    {
      user: limited
      password: limited123
      permissions: {
        publish: {
          allow: ["public.>", "events.>"]
          deny: ["events.private"]
        }
        subscribe: {
          allow: ["client.>", "events.>"]
        }
      }
    }
    {
      user: readonly
      password: readonly123
      permissions: {
        publish: {
          deny: ">"
        }
        subscribe: {
          allow: ">"
        }
      }
    }
  ]
}
